# Test fixtures

Recorded, redacted vendor responses and local files used by provider tests.

One directory per provider (`claude/`, `codex/`, ...). Every fixture is a real
capture with account identifiers, tokens, and names replaced. Never commit an
unredacted capture.
